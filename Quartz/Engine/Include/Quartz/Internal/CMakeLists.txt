set(currentDir ${CMAKE_CURRENT_LIST_DIR})
set(internalHeaders
	${currentDir}/EngineTypes.hpp
	${currentDir}/EngineEnums.hpp
	${currentDir}/EngineIntrinsics.hpp
	${currentDir}/EngineDefines.hpp

	PARENT_SCOPE
)
